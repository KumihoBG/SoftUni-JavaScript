function solve(arrStr){
    let items = new Map();

    arrStr.forEach(line => {
        let [days, name] = arrStr.split(' ');
    });
    for (const line of arrStr) {
        let [days, name] = arrStr.split(' ');
        items.set()
    }
    
    
}

solve(['Monday Peter',
'Wednesday Bill',
'Monday Tim',
'Friday Tim']
);