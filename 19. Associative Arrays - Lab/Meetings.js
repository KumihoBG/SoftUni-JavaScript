function solve(arrStr){
    let items = new Map();

    arrStr.forEach(line => {
        let [days, name] = line.split(' ');
    });

    // for (const line of arrStr) {
    //     let [days, name] = line.split(' ');
    //     items.set()
    // }
    
    
}

solve(['Monday Peter',
'Wednesday Bill',
'Monday Tim',
'Friday Tim']
);