function concatenate(param1, param2, param3, param4){
    let firstName = param1;
    let lastName = param2;
    let age = param3;
    let town = param4;
    console.log(`You are ${firstName} ${lastName}, a ${age}-years old person from ${town}.`);
}

concatenate("Pavleta", "Taseva", "23", "Sliven");