function multiplication(n) {
    for (let a = n; a <= n; a++) {
        for (let b = 1; b <= 10; b++) {
           let result = a * b;
           console.log(`${a} X ${b} = ${result}`);
        }
    }
}

multiplication(5);