function characters(arg) {
    let sequence = arg;
    for (let i = 0; i < sequence.length; i++)  {
        let letter = sequence[i];
        console.log(letter);
    }
}

// characters("softuni");
characters("ice cream");